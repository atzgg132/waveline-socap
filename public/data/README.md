Runtime copy of Corpus `data/launches.json`.

The app fetches `/data/launches.json` at runtime. After editing Corpus, run
`npm run sync-data` (also hooked to `dev` and `build`) so this file stays in
lockstep. Do not invent rows here.
